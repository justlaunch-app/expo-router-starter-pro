/**
 * Vexo is not supported on Expo Go. If you want to use Vexo, you need to build a standalone app with EAS.
 * For analytics with Vexo you need to add the following code:
 *
 * import { vexo } from 'vexo-analytics';
 * vexo('YOUR_API_KEY');
 */

import 'expo-router/entry';
